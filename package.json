{
  "name": "buzzquizz-beeh",
  "version": "2.0.0",
  "private": true,
  "description": "BuzzQuizz — quizzes em JavaScript, sem framework",
  "scripts": {
    "start": "node scripts/serve.cjs",
    "test": "node --test tests/*.test.cjs",
    "build": "node scripts/build.cjs"
  },
  "engines": { "node": ">=22" }
}
